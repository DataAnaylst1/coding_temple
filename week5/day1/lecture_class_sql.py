# ERD Entity Relationship Diagram
# Let's say you have orders and reviews
# How do you link the two in commonality?
# ERDs maps out the relationship of a comnpany's database
# PK - Primary key is the unique identifier of a data, a non-repeating 
# FK - is typically how a table is linked to another table
# Think of it like a house 
# Table names should always be lowercased, because uf 
# we were to call a table within sql, case sensitivty will be taken into accoun. 
# Same for column names
# SQL
# Creating tables, we need to specify how we are going to create the tables based on the ERD we created
# We specify the names of the fields
# Query